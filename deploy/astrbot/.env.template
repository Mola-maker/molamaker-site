# molabot — AstrBot environment variables
# Copy to /opt/molabot/.env and fill in real values.
# chmod 600 /opt/molabot/.env

# QQ account number for NapCat login (scan QR code on first run)
NAPCO_QQ=

# API key for AstrBot HTTP endpoint (generate with: openssl rand -hex 32)
ASTRBOT_API_KEY=
