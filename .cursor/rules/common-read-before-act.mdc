---
description: "动工前必须先读全量核心代码、明确用户需求、定位真实 bug"
alwaysApply: true
---

# 先读再动（Read Before Act）

**禁止**在未完成下列三步之前写代码、改配置或重构：

## 1. 读完全部核心相关代码

针对当前任务，**完整阅读**（不是 grep 片段、不是只看 diff）所有相关文件：

- 用户点名的文件
- 调用链上下游（API route → lib → 组件 → 测试）
- 同功能的现有测试与类型定义

若涉及跨模块（如 Math 作图），至少覆盖：**入口 API、业务 lib、前端消费端、对应 test**。

读完后用 1–2 句说明：**数据从哪进、经过哪些函数、在哪输出**。

## 2. 明确用户需求

动笔前必须能回答：

- 用户要解决的**具体现象**是什么？（报错、空白画布、构建警告…）
- **成功标准**是什么？（能作图、警告消失、测试通过…）
- 哪些是**明确禁止**的？（不换模型、不扩大改动范围、不回滚…）

若需求含糊，**先问用户**，不要假设。

## 3. 定位真实 bug（有证据）

- 必须指出**可复现的代码路径**或**失败的测试/构建输出**
- 区分：**逻辑 bug** vs **环境/配置** vs **模型行为** vs **误判**
- **禁止**在无证据时归因于「模型太弱」「用户配置不对」

未找到根因时：继续读代码或跑 discriminating check，**不要**先改一版试试。

---

## 动工门槛（全部满足才可写代码）

```
[ ] 已读完全部核心相关文件（可列出路径）
[ ] 已用一句话复述用户需求与成功标准
[ ] 已陈述根因假设 + 支撑证据（行号、测试名、日志）
[ ] 改动范围已收窄到根因相关文件，无无关 refactor
```

## 反模式（本仓库已发生过，禁止再犯）

- 未读 `route.ts` / `workplace-math.tsx` 全链路就改 prompt 或换模型
- 把 `deepseek-v4-flash` 等用户所选模型从纠错链里踢掉
- 修 Math 作图 bug 时顺手改 `middleware`/`i18n`/settings 等无关基础设施
- 同一假设失败三次仍重复 patch，不重新 capture 失败状态

## 例外

- 用户明确说「只改这一行 / 只加这个 rule / 只跑命令」→ 可跳过全量阅读，但仍需确认范围
- 纯文档、纯格式化、用户已给出完整 patch → 按用户指示执行
